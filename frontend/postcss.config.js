import tailwind from '@tailwindcss/postcss';
import autoprefixer from 'autoprefixer';

export default {
  plugins: {
    tailwindcss: {}, // Use the plugin name as the key
    autoprefixer: {}, // Use the plugin name as the key
  },
}